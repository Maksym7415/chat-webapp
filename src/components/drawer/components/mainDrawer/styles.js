import { makeStyles } from "@mui/styles";

export default makeStyles((theme) => ({
  list: {
    // width: 150,
    // [theme.breakpoints.up("sm")]: {
    //   width: 250,
    // },
  },
  wrapperlangs: {
    padding: 15,
  },
  listItem: {
    width: "100%",
  },
}));
