import { makeStyles } from "@mui/styles";

export default makeStyles((theme) => ({
  container: {
    width: "max-content",
    minWidth: 300,
  },
  wrapperBtn: {
    marginTop: 15,
    display: "flex",
    justifyContent: "center",
  },
  title: {
    textAlign: "center",
  },
}));
