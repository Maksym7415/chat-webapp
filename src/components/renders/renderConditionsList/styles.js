import { makeStyles } from "@mui/styles";

export default makeStyles((theme) => ({
  boxCenter: {
    display: "flex",
    alignItems: "center",
    justifyContent: "center",
    // flex: 1,
    height: "100%",
  },
  noResults: {
    fontSize: 20,
    fontWeight: "600",
  },
}));
