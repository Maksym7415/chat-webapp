import { makeStyles } from "@mui/styles";

export default makeStyles((theme) => ({
  copyWrapper: {
    display: "flex",
    alignItems: "center",
    flexDirection: "row",
  },
  copyText: {
    marginHorizontal: 0,
    marginVertical: 0,
    color: "#ffffff",
  },
}));
