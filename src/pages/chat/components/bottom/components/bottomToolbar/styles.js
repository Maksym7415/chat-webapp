import { makeStyles } from "@mui/styles";

export default makeStyles((theme) => ({
  root: {
    // width: "80%",
    display: "flex",
    justifyContent: "center",
    padding: "10px 0",
  },
  wrapper: {
    width: "80%",
    maxWidth: "500px",
  },
}));
