import { makeStyles } from "@mui/styles";

export default makeStyles((theme) => ({
  container: {
    overflowY: "auto",
    overflowX: "hidden",
    padding: "0px 10px 10px",
    height: "100%",
    position: "relative",
  },
}));
