import { makeStyles } from "@mui/styles";

export default makeStyles((theme) => ({
  container: {
    overflowY: "scroll",
    overflowX: "hidden",
    padding: "10px 0 0 10px",
    height: "100%",
    position: "relative",
  },
}));
