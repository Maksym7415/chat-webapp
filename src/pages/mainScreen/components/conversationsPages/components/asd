deleteFileWrapper: {
    width: '20px',
    height: '20px',
    position: 'absolute',
    top: 0,
    rigth: 0,
  },
  wrapperFile: {
    position: 'relitive',
  },

  <div className={classes.wrapperFile}>
                    <img key={i} className='conversations__upload-image' src={file.file} />
                    <div className={classes.deleteFileWrapper}>
                      <IconButton
                        color="primary"
                        aria-label="upload picture"
                        component="span"
                        className='carousel__options__upload-container__btn'
                        onClick={handleRemoveFile}
                      >
                        <DeleteIcon />
                      </IconButton>
                  </div>
              </div>